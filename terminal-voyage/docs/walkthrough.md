# Terminal Voyage - Solution Walkthrough

## Commands Used
- `pwd` - Verify directory location
- `ls -la` - Uncover hidden files and metadata
- `grep -rn "FLAG{" .` - Search recursively for hidden flags
- `chmod +x solve.sh` - Grant execution permissions
